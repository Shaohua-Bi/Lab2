import React from 'react';
import '../App.css';

const Home = () => {
	return (
		<div>
			<p>
				This is a simple example of using React to Query the Pokemon API from backend. Start by clicking the "Pokemon List", and "Trainers" button above.
			</p>
		</div>
	);
};

export default Home;
